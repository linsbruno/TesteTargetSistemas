let INDICE = 12;
        let SOMA = 0;
        let K = 1;

        // Loop enquanto K < INDICE
        while (K < INDICE) {
            K = K + 1; // Incrementa K
            SOMA = SOMA + K; // Atualiza SOMA com o valor de K
        }

        // Exibe o resultado
        console.log(SOMA);